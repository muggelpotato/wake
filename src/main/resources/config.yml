# whatever floats your boat
language: en_us
modules:
  base:
    enabled: true
  obu:
    enabled: true
  drydock:
    enabled: true
  axiom:
    enabled: true
database:
  type: "sqlite" # sqlite, mariadb
  host: "localhost"
  port: 3306
  database: "wake"
  username: "root"
  password: "password"
sync: # cross-server cache invalidation via Valkey pub-sub (requires shared mariadb database)
  enabled: false
  verbose_logging: false
  redis:
    host: "localhost"
    port: 6379
    password: ""